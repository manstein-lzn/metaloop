interface:
  display_name: "MetaLoop"
  short_description: "Design, verify, and resume Codex tasks"
  default_prompt: "Use $metaloop. Infer the task shape from the user's goal, ask only blocking questions, design the Mission Capsule and verification protocol before execution, then guide run/verify/repair/redesign through .metaloop artifacts."
policy:
  allow_implicit_invocation: true
