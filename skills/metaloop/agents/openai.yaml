interface:
  display_name: "MetaLoop"
  short_description: "Keep long Codex work recoverable with minimal overhead"
  default_prompt: "Use $metaloop to keep this resumable task implementation-first, finish it through the two-command path, and add review only if the final claim truly requires it."
